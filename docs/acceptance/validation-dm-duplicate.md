# Дубликат Data Matrix: приёмка и включение

Дата программной проверки: 2026-09-08. Ветка: `codex/validation-dm-duplicate`.
Объём: одна единица продукции внутри упаковки — тот же полный Data Matrix снаружи.
Новая функция прошла программную и браузерную приёмку. Дополнительный общий тест
выявил существующую ошибку инвентаризации, описанную ниже. Аппаратная приёмка
существующего парка оборудования имеет статус `not_run`. Режим на линии не включён.
Задачи 1–15 зафиксированы в `5f65bf89f4d029f344d7521525a1b62c016946c0`;
заключительные проверки и исправления функции — в `bcc80283e`. Последующие
уточнения шаблонов и их проверки описаны ниже.

## Зафиксированное поведение

Администратор в кабинете и оператор на станции задают печать и обязательность
проверки до открытия смены. Без печати сохраняется прежний сценарий. Параметры
открытой смены, шаблон и данные этикетки фиксируются; последующие изменения
библиотеки шаблонов не меняют уже подготовленные этикетки.

При обязательной проверке следующая единица недоступна, пока полное содержимое
кода новой этикетки не совпадёт с исходным и подтверждение не сохранится локально.
Пауза сохраняет задание. Без обязательной проверки можно продолжать после
сохранения результата отправки, но интерфейс не называет этикетку проверенной.

Неизвестный результат отправки после сбоя/перезапуска не вызывает автоматическую
печать. Оператор сканирует вышедшую этикетку либо явно повторяет печать с причиной
«не напечатана», «повреждена» или «потеряна». Повтор использует сохранённые байты,
включая первоначальную дату, и не увеличивает количество принятой продукции.

В кабинете отдельно видны отправки, проверки, незавершённые задания и повторные
попытки. Полный Data Matrix не показывается в истории; используются безопасный
суффикс и идентичность станции/задания. Серверные конфликты сохраняются в журнале.

## Программные доказательства

- Domain: разбор полного GS1-кода (AI93 и AI91+92), сохранение разделителя GS,
  порядка AI, регистра и криптохвоста, сравнение полного содержимого, неизменность
  повторной печати, GS1-растр и обе команды ZPL/TSPL при 203/300 dpi.
- SQLite: запись приёмки и зависимых фактов одним statement, конкуренция двух
  соединений, потеря ответа после commit, восстановление, заморозка snapshot,
  запрет закрытия при незавершённой этикетке, смена credential generation,
  очистка только после всех ACK. Это не физическое отключение питания.
- API/Postgres: миграции на отдельной временной локальной БД, изоляция tenant/device,
  старые станции, frozen policy, выключенный gate, точные receipts, повторная
  доставка, quarantine, история и пагинация одинаковых jobId разных станций.
- Station sync: сервер без явного receipt не удаляет ни scan, ни label events;
  закреплённый полный batch повторяется неизменно; позднее verified остаётся для
  следующей доставки. Смена организации не переносит локальные задания другой.
- Browser: реальные кабинетные маршруты и `NewShift`/`WorkScreen`; API и принтер
  **mock**, журнал — отдельная настоящая SQLite с двумя чередующимися соединениями.
  Перезагрузка страницы сохраняет журнал. Scanner fixture входит через штатный
  `createHardwareScanSource`, без физического HID/COM устройства.
- Геометрия: кабинет 1920×1080, станция 1280×800; русский/английский, светлая/тёмная
  темы. Проверены обязательное окно, отсутствие пропуска, фокус, действия 64 px,
  отсутствие внешней прокрутки на станции. Галерея дополнительных состояний
  использует production-компоненты с синтетическими состояниями, не исполняет печать.

Проверка обнаружила ограничение окна проверки шириной небольшого уведомления.
Диалоги новой операции вынесены из контейнера уведомлений. Регрессионная проверка
теперь требует фактическую ширину 1280 px; существующие уведомления не изменены.

Дополнительно проверены настройки принтера на 1024×768: разрешение и язык печати
должны читаться полностью, checkbox и основные действия остаются в видимой области.
Карточка дубликата использует отступы и типографику существующих приборов Station.
Подсказка до первого скана берёт обязательность проверки из сохранённой политики
смены, даже когда первого задания ещё нет. Эти дефекты подтверждены падающими
проверками до исправления.

### Итоговые результаты

| Область                                                 | Тесты                        | Типы, lint, сборка                                |
| ------------------------------------------------------- | ---------------------------- | ------------------------------------------------- |
| Domain                                                  | passed: 622                  | passed                                            |
| DB, отдельный локальный Postgres                        | passed: 372                  | passed                                            |
| Platform contracts                                      | passed: 81                   | passed                                            |
| UI / Email / Legal documents                            | passed: 171 / 23 / 146       | passed                                            |
| API, основной полный прогон                             | passed: 2897, skipped: 25    | passed                                            |
| API, дополнительные inventory suites с отдельным URL БД | passed: 21, failed: 1        | существующий сбой ниже                            |
| Admin                                                   | passed: 1002                 | passed, 5 прежних lint warnings в boxes/conflicts |
| Station                                                 | passed: 1388                 | passed                                            |
| Kiosk / SaaS Admin / Signer UI / Landing                | passed: 617 / 241 / 34 / 160 | passed                                            |
| Browser, Chromium                                       | passed: 23, retries: 0       | отдельный typecheck passed                        |
| Production bundle contracts                             | passed: 536                  | contract gate passed                              |

После основного API прогона 22 из 25 пропущенных тестов отдельно запущены с
`INVENTORY_TEST_DATABASE_URL` в новой временной БД: 21 прошёл, один выявил ошибку
порядка событий. Оставшиеся три opt-in проверки не выполнялись: Mailpit/MinIO,
provisioning CLI с `LOCAL_INFRA_SMOKE=1` и live National Catalog с внешней
конфигурацией. Они не заменены mock-результатами. Все проверки новой функции
с Postgres выполнялись на настоящей изолированной локальной БД.

Исходная ошибка английских plural keys исправлена, полный Admin повторён.
Форматирование всего репозитория и `git diff --check`: passed перед коммитом.
Изменений Rust нет, Cargo/Windows/Tauri на настоящем устройстве не запускались.
Graphify graph в рабочей копии отсутствует; чужой граф исходной копии не обновлялся.

Снимки и трассы находятся вне Git, в `tmpdir()/markiro-dm-browser`; выбранные
desktop-снимки сохранены в артефактах задачи. Они содержат синтетические данные.
Визуально просмотрены кабинет 1920×1080, создание смены оператором, обязательная
проверка, ожидание без проверки и настройки принтера 1024×768. Референс холста
`check_with_reprint.pen` прочитан только через Pencil MCP.

### Существующий сбой инвентаризации

`apps/api/test/station-inventory-sync.e2e.test.ts`, сценарий
`owns repack boxes and membership on the server across real competing devices`,
нестабилен: если более поздний скан станции B уже размещён в коробе с датой
2026-08-22, а затем приходит более ранний победивший скан A с датой 2026-08-20,
обновление `inventory_code_results.observed_production_date` опережает удаление
прежнего членства. Postgres отклоняет его с `23503` по
`inventory_repack_items_tenant_result_active_date_fk`.

Происхождение проверено на исходном commit
`c2e0ef01871e89f6ca8a1d519df0b4893d0526a3`: обычный тест может пройти; временный
probe меняет только порядок доставки с `Promise.all` на B → A и воспроизводит
тот же FK-сбой в исходной версии. Временный probe удалён, исходный checkout
сохранён. Inventory service, исходный тест и его миграции эта ветка не меняет.
Исправление относится к отдельной задаче переупаковки; весь расширенный API gate
не объявляется зелёным. Это не аппаратное подтверждение новой печати и не
разрешение выпуска.

## Обновление штатной этикетки 58×40

По уточнению от 2026-09-08 штатный шаблон использует стиль существующих этикеток:
полужирное название и тонкие разделители. Data Matrix размером 24×24 мм расположен
справа; слева — название для печати, даты розлива и годности, код ЕГАИС и текстовая
идентичность маркировки `01 + GTIN + 21 + serial` без криптохвоста. Текст может
переноситься на две строки. В Data Matrix, сохранённых заданиях и проверке остаётся
полное исходное содержимое, включая GS и криптографические AI.

Общий формат поля `textFormat: km_without_crypto` применяется только к текстовому
`km.code`; прежние шаблоны без этого параметра сохраняют прежнее представление.
Миграция 0114 обновляет только неизменённый штатный шаблон из 0113 с исходным
названием и назначением. Пользовательские изменения, переименованные шаблоны,
выключенное состояние и снимки открытых смен сохраняются. Миграция проверена на
отдельном локальном Postgres, вне него в этой задаче не применялась.

При визуальной проверке обнаружена загрузка только латинского подмножества шрифта
на первом рендере. Растеризаторы кабинета и станции теперь ожидают шрифт для
фактического текста. Два браузерных теста сначала воспроизвели различие, затем
подтвердили совпадение первого и повторного кириллического растра.

Повторные проверки этого изменения: Domain — 628, DB — 373, Admin — 1002,
Station — 1388, API-потребители шаблонов/политики/событий/истории — 64 теста;
Chromium — 25 сценариев без повторов. Все эти тесты прошли без пропусков.
Типы, lint и сборки затронутых пакетов, форматирование всего репозитория и
`git diff --check` прошли; прежние пять предупреждений lint кабинета сохраняются.
Ранее выявленная ошибка инвентаризации выше остаётся отдельным ограничением.

Визуально просмотрен PNG действующего `PreviewPane` с синтетическими данными;
снимки сделаны с обоими растеризаторами. Аппаратная печать и сканирование новой
компоновки не выполнялись. Применение на линиях — отдельный этап выпуска.

## Два разрешения штатной этикетки

Следующее уточнение от 2026-09-08 добавляет две отдельные записи библиотеки:
«Дубликат Data Matrix 58×40 (203 dpi)» и «Дубликат Data Matrix 58×40 (300 dpi)».
Геометрия в миллиметрах одинакова. Заголовок обоих шаблонов привязан к краткому
`product.printName` — полю каталога «Наименование для печати». Проверка подготовки
настоящих ZPL-байтов использует разные полное и краткое названия и подтверждает,
что на этикетку попадает краткое. Размеры команды печати: 464×320 точек для 203 dpi
и 685×472 для 300 dpi.

Миграция 0115 добавляет отсутствующие варианты существующим организациям;
создание новой организации сразу создаёт оба. Повтор миграции/создания не
дублирует записи. Сохранены пользовательские изменения, выключенные шаблоны,
одноимённые шаблоны коробов и выбор этикетки короба по умолчанию. Применение
миграции подтверждено только в изолированных тестовых БД.

Существующая подстановка полного названия при пустом `product.printName` пока
сохранена; поведение для незаполненного краткого названия уточняется отдельно.
Автоматическое сокращение полного названия не добавлялось.

Повторные проверки этого уточнения: Domain — 629, DB — 374, Station — 1390 тестов;
потребители в Admin — 63 теста. Все прошли. Browser — четыре сценария: оба DPI
с растеризаторами кабинета и станции, без повторов. Типы браузерных тестов прошли.
Оба PNG получены из рабочего предпросмотра на синтетических данных; вариант
300 dpi визуально просмотрен. Печать на оборудовании линии не выполнялась:
она остаётся отдельной аппаратной приёмкой. Публикация PR не включает применение
миграций на линиях.

Полный API-прогон после добавления двух разрешений: 2897 passed, 25 skipped.
Пропуски относятся к прежним opt-in проверкам с отдельными инфраструктурными
переменными (условия перечислены выше); расширенная проверка инвентаризации
повторно не запускалась, её известный сбой не объявляется исправленным.
Типы, lint и сборки Domain, DB, Station и API прошли. Форматирование всего
репозитория и `git diff --check` прошли. Проверки запускаются установленными
CLI напрямую по описанному выше ограничению pnpm.

## Повтор программной приёмки

Использовать Node 24+, зависимости из lockfile и отдельную тестовую БД.
Сначала собрать domain и db, затем выполнить serial lint/typecheck/test/build
всех workspace-пакетов с переменными тестовой инфраструктуры по AGENTS.md.
После этого:

```sh
pnpm --dir tools/production-browser run test:product-labels
pnpm test:production-bundle:contract
pnpm format:check
git diff --check
```

Браузерный сценарий запускает отдельные локальные серверы на 43181/43182,
`strictPort`, `reuseExistingServer:false`, один worker, без retries. Middleware
SQLite подключён только тестовой конфигурацией Vite, ограничен loopback origin
и временными файлами; в production entrypoint его нет. Каждый сценарий получает
свою БД, соединения закрываются и временные файлы удаляются при остановке сервера.

В текущем worktree pnpm запуск блокируется существующей записью packageManager
в lockfile. Проверки запускаются непосредственно установленными Vitest, tsc,
ESLint, Vite/Nest/Astro и Playwright тех же версий. Lockfile, ограничения возраста
релизов и конфигурация production-окружения для обхода не менялись.

## Исправления CI в PR #457

Прогон `34212023636` на `edecefdd493415555456f17eacc15a054c4889a4`
обнаружил три дефекта: отсутствие зависимости тестов DB от Domain в графе сборки,
неполный список маршрутов в release-проверке CORS и обрезанные настройки языка
принтера при высоте рабочей области 600 px.

DB объявляет Domain как devDependency, а миграционные тесты используют публичный
импорт `@markiro/domain`. Граф Turbo теперь требует `domain#build` перед
`db#typecheck`. Release-проверка и обе независимые фикстуры дополнены маршрутами
`GET /shifts/planning-config` и `GET /shifts/product-label-templates`.

На низком экране язык расположен рядом с подключением, а нижние настройки
доступны через прокрутку карточки. На 1024×768 сохраняется широкая форма.
Исходная браузерная проверка усилена: в видимой области должна быть вся кнопка
ZPL/TSPL. Дополнительные сценарии проверяют выбор обоих DPI и переключатель
проверки на 1024×600, 1024×768 и 1280×800.

Локальные результаты после исправлений: DB — 374 passed на отдельном Postgres,
Station — 1390 passed, release contracts — 327 passed, production bundle
contracts — 536 passed. Типы, lint и сборки DB/Station прошли. Полный браузерный
набор станции — 7 passed; после уточнения CSS повторены все 4 сценария настроек.
Аппаратная матрица ниже от этих проверок не меняется.

В CI `34216109474` Windows-сборка станции и все 7 браузерных сценариев прошли.
Следующий этап обнаружил пропуск `VALIDATION_DM_DUPLICATE_ENABLED` в независимом
перечне production-переменных теста Yandex runtime. Перечень дополнен;
материализация окружения по-прежнему требует точного совпадения ключей.

## Аппаратная матрица

Используется существующий разнородный парк, отдельный новый перечень моделей
не утверждён. Заполнять по каждой реально испытанной комбинации. Допустимы только
`passed`, `failed`, `not_run`; отсутствие оборудования не означает совместимость.

| Проверка                                                     | Результат | Причина / что записать при выполнении                                                |
| ------------------------------------------------------------ | --------- | ------------------------------------------------------------------------------------ |
| Windows/Tauri и настоящий пул SQL, перезапуск станции        | not_run   | В этой сессии Windows-станция не подключалась; записать версию ОС, Tauri и build SHA |
| Существующий принтер ZPL, 203 dpi, USB/очередь Windows       | not_run   | Записать модель, firmware, драйвер, носитель 58×40 и ID теста                        |
| Существующий принтер ZPL, 300 dpi, сеть/serial               | not_run   | Испытать каждое фактически используемое подключение                                  |
| Существующий принтер TSPL, 203 dpi, USB/сеть/serial          | not_run   | Записать конкретную комбинацию модели и подключения                                  |
| Существующий принтер TSPL, 300 dpi, USB/сеть/serial          | not_run   | Не переносить результат одного разрешения на другое                                  |
| Сканеры HID и COM                                            | not_run   | Модель, настройки передачи GS и AIM identifier, полное совпадение кода               |
| Кириллица, модуль Data Matrix, quiet zone, реальный носитель | not_run   | Оценить читаемость новой этикетки при производственной скорости                      |
| Потеря питания/USB/сети до, во время и после send            | not_run   | Настоящие отключения, сохранность журнала, отсутствие auto resend                    |

Протокол для каждой комбинации:

1. Зафиксировать commit, станцию, оператора, Windows/Tauri, принтер/firmware,
   сканер, подключение, язык, DPI, носитель и synthetic test ID.
2. Подать синтетические полные коды отдельно с AI93 и AI91+92. Включить GS,
   кавычки, скобки, `^`, смешанный регистр; поля этикетки — с кириллицей.
3. Напечатать, отсканировать этикетку штатным сканером и проверить
   `compareDuplicateKm(original, scanned) === "match"`. Сравнивать полное
   содержимое, а не только GTIN/серийный номер.
4. Повторить после смены календарной даты: байты и дата исходной этикетки
   сохраняются. Количество продукции не растёт; номер попытки растёт.
5. Испытать обрыв до send, во время send и после send до сохранения результата;
   также завершить процесс/отключить питание в соответствующих точках.
   После восстановления возможна проверка/явный повтор, автоматического повтора нет.
6. Пройти required/none, паузу, смену оператора, закрытие, offline/reconnect,
   повторную доставку batch и восстановление уже закрытой удалённо смены.
7. В журнале приёмки указать результат каждого теста. Реальные товарные коды,
   credentials и ключи не добавлять в Git/скриншоты.

## Порядок выпуска и откат

1. До выпуска дополнить production runtime inventory/Lockbox ключом
   `VALIDATION_DM_DUPLICATE_ENABLED` со значением `false` и проверить точное
   совпадение с `.env.production.example`. Применить совместимые добавочные
   миграции Postgres и API. Оставить
   `VALIDATION_DM_DUPLICATE_ENABLED=false` (значение по умолчанию).
2. Поставить совместимую Station с протоколом `validation-dm-duplicate-v1` и
   добавочными SQLite migrations. Проверить восстановление старых очередей.
3. Пройти аппаратную матрицу на конкретных линиях, затем отдельно разрешить
   включение флага. Кабинет/станция рекламируют создание режима только при
   включённом флаге; обычные смены продолжают работать при выключенном.
4. При проблеме выключить gate: новые duplicate-смены не создаются, но bundle,
   recovery и sync уже существующих заданий продолжают работать. Это не команда
   удаления очередей, отмены обязательной проверки или повторной печати.
5. Не понижать Station до версии, не понимающей новый журнал: старый бинарник не
   имеет механизма защиты от такого downgrade. Восстановление — совместимым
   исправлением. Не удалять SQLite/очереди и не откатывать применённую миграцию.

Установка на линиях и включение флага требуют отдельного поручения. Публикация
ветки и PR не означает разрешения этих действий; программная приёмка их не включает.
