# Киоск «Для себя» — Plan B: приложение и офлайн — дизайн-спека

> Статус: утверждён к планированию (2026-07-24);
> пересмотрен 2026-07-28 под состояние `main` после планов 05b-1/2/3
> (см. §6.1–6.2 бейдж-хэши, §5.3 операторские креды, §10 триаж device-key).
> Продолжение плана A (`docs/superpowers/specs/2026-07-23-pickup-kiosk-design.md`,
> смержен PR #5) — там поставлены данные, device-API и админка.
> Смежный дизайн: `docs/design-briefs/07-device-commissioning.md` и
> `docs/superpowers/specs/2026-07-24-device-commissioning-design.md` (привязка
> устройств; ветка `docs/device-commissioning-brief`).
> Прототип экранов: `docs/design-briefs/design_handoff_markiro/prototypes/pickup-kiosk.dc.html`.
> Следующий шаг: writing-plans → план в `docs/superpowers/plans/`.

## 1. Обзор и цель

Plan B поставляет **само устройство** — сенсорный киоск самообслуживания
`apps/kiosk` у проходной цеха/склада. Сотрудник сканирует QR-бейдж, затем коды
маркировки «Честный ЗНАК» с продукции, которую забирает для себя; киоск
формирует заявку и передаёт её на сервер. Серверная часть (заявки, лимиты,
дедуп, админка) уже работает — она поставлена планом A.

Ключевое требование: киоск **работает офлайн** (цех может остаться без сети) и
**запускается на любой платформе**, включая Android-планшеты.

## 2. Зафиксированные решения (decision log)

1. **Платформа — веб-ядро (PWA), а не нативная оболочка.** `apps/kiosk` —
   React 19 + Vite 8 + TS, PWA (service worker, шрифты bundled, без CDN).
   Работает на Android/iOS-планшетах и сенсорных моноблоках Windows/Linux.
   _Обоснование:_ требование «все платформы, включая Android». Tauri (как в
   `apps/station`) дал бы OS-lockdown и serial, но не покрывает планшеты:
   serial на Android требует нативного USB-Host-плагина, на iOS практически
   нереализуем.
2. **Tauri-оболочка — отдельный поздний шаг.** Тот же фронтенд позже
   заворачивается в Tauri для десктоп-моноблоков (kiosk-lockdown, updater,
   serial через Rust) по образцу `apps/station`. Абстракция транспорта сканера
   делает это добавлением оболочки, а не переписыванием. **Вне Plan B.**
3. **Сканер — плагаемый транспорт.** `KeyboardWedgeSource` (USB-OTG/Bluetooth
   HID) — везде, включая планшеты; `WebSerialSource` — где доступен
   `navigator.serial` (десктоп-Chromium), сырые байты с разделителем GS.
   Поверх — обязательный доменный guard (§6).
4. **Хранилище — IndexedDB.** Одна реализация на все платформы (работает и
   внутри Tauri-вебвью). SQLite-зеркало станции здесь избыточно: объём данных
   мал (снимок справочников + короткая очередь заявок).
5. **`apps/kiosk` целиком принадлежит Plan B** — рабочие экраны, офлайн,
   транспорт сканера, **экран привязки** (дизайн 07, screen 3) и **экран
   настройки сканера** (дизайн 07, screen 5).
6. **Инициализация данных происходит при привязке.** Обмен pairing-кода
   возвращает и креденшел, и полный стартовый набор справочников — киоск
   сразу готов к работе (согласовано с треком 07).
7. **Серверный pairing — минимальный, только для киоска, внутри Plan B.**
   Одноразовый числовой код с TTL и серверным lockout строго по контракту
   дизайн-брифа 07 (§5.1); обмен возвращает **существующий `x-kiosk-token`**.
   Серверная часть плана A не переделывается. Распространение того же механизма
   на станции и кабинетный раздел «Устройства» — за отдельным планом
   commissioning (§4).
8. **На устройство отгружаются только PBKDF2-хэши бейджей, не плейнтекст.**
   `KioskBootstrapDto.employees[].badgeCodes` заменяется на `badgeHash`; киоск
   сверяет отсканированный бейдж локально. Это требование политики
   `docs/device-key-surface.md`, уже соблюдаемое станцией (§6.1).
9. **Настройки после привязки — за операторскими кредами**, а не за отдельным
   сервисным PIN: реестр операторов появился в 05b-1, и бриф 07 §5 требует
   именно его (§5.3).
10. **Триаж device-key-поверхности киоска — обязанность Plan B.**
    `docs/device-key-surface.md` явно закрепляет за этим воркстримом
    классификацию модулей `kiosks` / `pickup-orders` / `pickup-reasons` (§10).
11. **Конфликты синхронизации сохраняются на заявке** и показываются админу
    (иначе отвалившиеся при синке коды исчезают бесследно — см. §7.1).
12. **Политика устаревшего кэша:** предупреждение через 24 ч, блокировка новых
    заявок через 7 суток; пороги — константы сборки (§7.3).

## 3. Границы

**В Plan B:**

- приложение `apps/kiosk`: рабочие экраны (Ожидание / Отбор / Передано),
  экран привязки, экран настройки сканера, экран блокировки;
- офлайн: кэш справочников, очередь заявок, фоновая синхронизация;
- транспорт сканера (клавиатурный + Web Serial) и доменный guard;
- минимальный серверный pairing **для киоска** (выдача кода в кабинете + обмен
  на устройстве);
- **перевод kiosk-bootstrap на хэши бейджей** + общий PHC-верификатор (§6.1);
- **триаж device-key-поверхности** `kiosks`/`pickup-orders`/`pickup-reasons`
  и обновление `docs/device-key-surface.md` (§10);
- персист конфликтов синхронизации на заявке + их отображение в админке;
- строки про pickup-киоск (планы A и B) в роадмапе (§4).

**Вне Plan B:**

- кабинетный раздел «Устройства», квота подписки, печатный лист инструкции,
  привязка станции — за отдельным планом commissioning по брифу 07;
- Tauri-оболочка (десктоп-lockdown, updater, serial через Rust);
- CommerceML; прямой вывод из оборота через ГИС МТ/СУЗ.

## 4. Место в роадмапе и зависимости

Зависит от:

- **Plan A** (в `main`): device-API `/kiosk/bootstrap` + `/kiosk/orders`,
  `KioskDeviceGuard` (`x-kiosk-token`), таблицы `kiosks`/`kiosk_products`/
  `pickup_orders`/`pickup_order_items`/`employees`/`employee_badges`, доменные
  `classifyScan`/`validatePickupKm`/`kmKey`.
- **Дизайн-бриф 07 «Device commissioning»** (`docs/design-briefs/07-device-commissioning.md`
  и одноимённая спека в `docs/superpowers/specs/`): экраны привязки и настройки
  сканера, контракт pairing-кода. **Только дизайн — реализации нет**
  (`grep pairing` по `apps/` пуст).
- **План 05b-1 «Реестр операторов»** (в `main`): `operator_credentials`
  (1:1 к `employees`), `employee_badges.badge_hash`, PHC-хэши
  (`apps/api/src/lib/pin-hash.ts` ↔ `apps/station/src/lib/crypto.ts`),
  `GET /station/operators`. Отсюда киоск берёт и модель бейдж-хэшей (§6.1), и
  операторские креды для настроек (§5.3).

> **Осторожно с номером «07».** В роадмапе `07` — это «Exports, history &
> dashboard». Device commissioning — это дизайн-**бриф** 07, другой документ.
> В этой спеке под «07» всегда понимается бриф commissioning.

**Не зависим по файлам от 05b, но зависим по контрактам:** 05b-1 владеет
моделью бейдж-хэшей, 05b-3 — политикой device-креденшелов
(`docs/device-key-surface.md`) и атомарной публикацией реестра
(`replaceOperatorsMirror`, две слот-таблицы).

**Точка синхронизации с планом commissioning.** Plan B реализует pairing
**только для киоска** и ровно по контракту §5.1 из брифа 07 (8 цифр, TTL
15 мин, lockout 5 попыток, одноразовость, provisioning-бандл). Отдельный план
commissioning затем расширяет тот же механизм на станции и добавляет
кабинетную часть («Устройства», квота, печатный лист). Формат кода, TTL,
lockout и понятие бандла — общие; менять их этот план может, но тогда меняет и
клиент киоска. _(Спека 07 в разделе Scope boundary претендует на реализацию
для обоих типов устройств одним планом; поскольку без привязки киоск
неработоспособен, а реализации 07 нет, разделение зафиксировано в пользу
Plan B.)_

**Роадмап.** `docs/superpowers/plans/2026-07-21-markiro-mvp-roadmap.md` не
содержит ни одной строки про pickup-киоск, хотя план A уже поставлен (PR #5).
Plan B добавляет две строки: pickup-kiosk A (сделано) и B (эта поставка).

## 5. Привязка устройства (pairing)

### 5.1 Серверная часть (минимум)

Новая таблица `kiosk_pairing_codes`: `id, tenant_id, kiosk_id, code_hash,
expires_at, used_at, attempts, created_at`; композитный FK на
`kiosks(tenant_id, id)` по общему паттерну.

- **`POST /kiosks/:id/pairing-code`** (админ-сессия, `TenantGuard`) — выдаёт
  одноразовый **8-значный числовой** код, TTL **15 минут**. Хранится только
  хэш. Повторный вызов инвалидирует предыдущий код. Ответ: `{ code, expiresAt }`
  (плейнтекст показывается один раз).
- **`POST /kiosk/pair`** (без аутентификации) `{ code, serverUrl? }` — сверяет
  код: не использован, не истёк, лимит попыток не исчерпан. Успех → помечает
  код использованным, выпускает device-токен через существующий
  `generateDeviceToken()`/`hashDeviceToken()` и возвращает **provisioning-бандл**:

```ts
{
  device: { kioskId: string; kioskName: string; place: string | null },
  token: string,                // существующий x-kiosk-token
  nextDeviceSeq: number,        // см. §7.2 — защита от коллизии после переустановки
  bootstrap: KioskBootstrapDto, // тот же тип, что отдаёт GET /kiosk/bootstrap (§6.1)
}
```

- **Защита от перебора:** серверный lockout после **5** неверных попыток на
  код (и на IP-окно); код претендует на заранее созданную запись киоска, что
  ограничивает пространство перебора.

Вложенный `bootstrap` — **ровно тот же `KioskBootstrapDto`**, что уже отдаёт
`GET /kiosk/bootstrap`. У киоска один путь приёма данных: и при инициализации,
и при каждом обновлении. Никакого второго формата.

### 5.2 Клиентская часть (дизайн 07, screen 3)

Экран «Привязка устройства»: числовая клавиатура для ввода кода + кнопка
«Сканировать код» (использует тот же `ScanSource`) + свёрнутое поле «Адрес
сервера» (для on-prem; в SaaS-сборке origin вшит в билд). Состояния:
идёт привязка → «Загружаем настройки и справочники»; неверный/истёкший код →
«Обновите код в кабинете»; слишком много попыток → «Подождите или получите
новый код»; нет сети → «Проверьте адрес и подключение», повтор. Успех →
«Киоск привязан к точке X» → рабочий режим.

### 5.3 Настройка сканера (дизайн 07, screen 5)

- **До привязки** — доступна без логина прямо с экрана привязки (сканер часто
  нужно поднять раньше, чтобы отсканировать pairing-код).
- **После привязки** — из настроек киоска **за операторскими кредами**: скан
  бейджа оператора **или** логин (табельный номер) + PIN. Проверка офлайн по
  PHC-хэшам из кэша (§6.1). Это ровно то, что требует бриф 07 §5, и та же
  модель, что у станции; отдельный «сервисный PIN» не вводится.
  _Ранее в этой спеке предполагался сервисный PIN — предпосылка «у киоска нет
  операторов» устарела: реестр `operator_credentials` появился в 05b-1._

Экран: выбор режима (клавиатурный / Web Serial + выбор порта), **тестовый
скан** с показом распознанного типа (бейдж / КМ / не распознано).

## 6. Сканирование и учётные данные на устройстве

**`ScanSource`** — общий интерфейс: подписка на поток `onScan(raw: string)`,
`start()`/`stop()`, статус доступности.

- **`KeyboardWedgeSource`** — глобальный слушатель клавиатуры: копит символы до
  терминатора (Enter) или таймаута тишины; отсеивает «человеческий» ввод по
  скорости. Работает на всех платформах.
- **`WebSerialSource`** — `navigator.serial`, чтение сырых байтов (GS
  сохраняется), авто-reconnect при обрыве. Доступен только на десктоп-Chromium;
  при отсутствии API источник объявляет себя недоступным и UI его не предлагает.

Каждое событие проходит через доменный слой (`@markiro/domain`, готов):

- `classifyScan(raw)` — структурный детект: бейдж (не-КМ) vs КМ;
- `validatePickupKm(raw)` — group-aware guard: `not_km` / `incomplete` / `ok`.
  `incomplete` (пропал GS у клавиатурного сканера) → «Код прочитан не
  полностью, поднесите ещё раз»;
- `kmKey(km)` — канонический ключ дедупа внутри корзины.

### 6.1 Бейдж-хэши вместо плейнтекста (изменение контракта плана A)

Сегодня `GET /kiosk/bootstrap` отдаёт `employees[].badgeCodes` **открытым
текстом**. Незапертый планшет у проходной — самый уязвимый к краже узел
системы, а бейдж и есть credential, которым авторизуется отбор. Проект уже
закрыл ровно эту дыру для станции: `/employees` спрятан за `SessionOnlyGuard`
именно потому, что «`EmployeeDto` несёт плейнтекст badge codes — то самое, что
предотвращает отгрузка на устройства только хэшей»
(`docs/device-key-surface.md`). Киоск остался исключением.

Plan B это закрывает:

- **`KioskBootstrapDto.employees[]`**: `badgeCodes: string[]` → `badgeHash: string | null`.
  Источник — `employee_badges.badge_hash` (добавлен в 05b-1) и уже готовый
  `activeBadgeHashes()` из `OperatorsService` (ленивый backfill, правило «самый
  свежий активный бейдж выигрывает»).
- **`KioskBootstrapDto.operators[]`** — новый список для доступа к настройкам
  (§5.3): `{ employeeId, name, login, pinHash, badgeHash, active }`, хэши и
  только хэши. Форма совпадает с `OperatorMirrorRecord` станции.
- **Проверка на устройстве** — PBKDF2 по PHC-контракту (`pbkdf2$sha256$100000$…`).
  Реализаций сейчас две (`apps/api/src/lib/pin-hash.ts`, строже; и
  `apps/station/src/lib/crypto.ts`), общего пакета нет. Чтобы киоск не стал
  третьей копией, Plan B **выносит верификатор в `packages/domain`** (чистая
  функция + WebCrypto) и потребляет его; миграция станции на общий модуль —
  необязательный бонус, не требование.
- **`POST /kiosk/orders` не меняется.** Киоск по-прежнему шлёт
  `badgeCode` — только что физически предъявленный сотрудником, а не хранимый
  реестр; сервер остаётся авторитетом и резолвит бейдж сам.

Изменение ломающее для `KioskBootstrapDto`, но единственный его потребитель —
киоск, которого ещё нет; внешних клиентов у контракта нет.

### 6.2 Сверка бейджа за одну деривацию (per-tenant salt)

Наивная сверка «скан против списка хэшей» линейна: станционный
`verifyOperatorBadge` (`apps/station/src/lib/auth.ts`) в цикле зовёт
`verifyBadge` на каждого. Для реестра операторов станции (десятки) это
терпимо, для киоска — нет: там в кэше **весь штат**, и при 100 000 итераций
PBKDF2 скан бейджа занял бы секунды-десятки секунд. Для терминала, где скан
должен отзываться мгновенно, это неприемлемо.

Решение: **соль для бейдж-верификаторов — одна на тенант** (не на строку).
Тогда киоск при скане:

1. берёт соль из любой записи кэша (она общая) и делает **одну** деривацию;
2. ищет полученный дайджест в `Map<digest, employeeId>`, построенной из кэша.

Итого: одна PBKDF2-операция на скан вместо N, независимо от размера штата.

Совместимость: PHC-строка по-прежнему несёт свою соль, поэтому станционный
`verifyBadge(code, hash)` продолжает работать без изменений — просто у всех
бейджей тенанта соль совпадает. Станция может позже перейти на тот же быстрый
путь; в Plan B это не требуется.

Компромисс, честно: общая соль означает, что одна предвычисленная таблица
атакует все бейджи тенанта сразу, а не по одному. При 100 000 итераций и
случайной соли на тенант это остаётся дорого, и главное — **PIN-верификаторы
солятся по-прежнему построчно** (там 4 цифры, там это критично). Бейджи —
длинные и выдаются системой, поэтому для них выбор в пользу общей соли
оправдан. Альтернатива (построчная соль + линейный перебор) отклонена как
непригодная по времени отклика.

## 7. Офлайн-модель

- **Кэш (IndexedDB):** снимок `KioskBootstrapDto` + `fetchedAt`. Пишется при
  привязке и при каждом успешном обновлении **атомарно, полной заменой**:
  удалённый на сервере сотрудник исчезает и из кэша, а частично записанный
  снимок никогда не виден читателю. Референс — `replaceOperatorsMirror`
  станции (публикация в неактивный слот и переключение указателя одним
  оператором), а **не** `upsertBundle`, который атомарности не даёт. В
  IndexedDB это одна read-write транзакция на весь снимок.
- **Обновление:** периодически при онлайне (интервал по умолчанию **5 минут**)
  и при возврате сети (событие `online`). Оно же служит **heartbeat** — любой
  device-вызов обновляет `kiosks.last_seen_at`, по которому админка показывает
  «в сети». Отдельного heartbeat-эндпоинта не нужно.
- **Валидация — всегда локальная**, одинаково онлайн и офлайн (отдельных
  онлайн-эндпоинтов резолва в плане A нет):
  - бейдж → сотрудник: **одна** PBKDF2-деривация скана и поиск результата по
    хэш-таблице кэша (§6.2); не найден → «Бейдж не распознан»;
  - КМ → guard, затем поиск `gtin14` среди `products` кэша (это уже allowlist
    киоска); не найден → **красная модалка** «Товара нет в каталоге,
    обратитесь к администратору»;
  - дедуп внутри корзины по `kmKey` → янтарный баннер «Этот код уже в списке»;
  - дневной лимит — **best-effort** по локальному журналу заявок киоска
    (сервер остаётся авторитетом).
- **Очередь:** заявка кладётся в `queue` с монотонным `deviceSeq` и `createdAt`
  (время отбора, не синка — сервер это поддерживает). Отправка строго по
  порядку `deviceSeq`, экспоненциальный backoff при ошибках. Идемпотентность
  серверная по `(tenant, kiosk, deviceSeq)` — повтор безопасен и возвращает ту
  же заявку.
- **Авторитет — сервер.** `orderNo` и `conflicts` приходят в ответе на
  `POST /kiosk/orders`. Онлайн-экран «Передано» показывает номер сразу; в
  офлайне — локальное подтверждение **без номера** («Заявка передана, номер
  появится после синхронизации»).
- **Возраст кэша:** >24 ч — ненавязчивая плашка «Данные обновлялись N назад»,
  отбор разрешён; >7 суток — экран `blocked` («Киоск не на связи, обратитесь к
  администратору»), новые заявки не принимаются, **очередь продолжает
  досылаться**. Оба порога настраиваемые.

### 7.1 Конфликты синхронизации (важно для офлайна)

Офлайн-заявка уходит на сервер спустя часы, когда сотрудник уже ушёл с
продукцией. Сервер может отклонить часть позиций (`duplicate`, `over_limit`,
`unknown_product`, `not_allowed`, `not_km`, `incomplete`) — заявка создастся
только с принятыми. Сейчас `conflicts` возвращаются в ответе и нигде не
сохраняются, поэтому админ не увидит расхождения.

Plan B добавляет:

- **сохранение конфликтов** при создании заявки (JSONB-поле на `pickup_orders`
  либо дочерняя таблица — выбирается при планировании) и их отдачу в
  `PickupOrderDetailDto`;
- **в админке** — плашка в карточке заявки: «При синхронизации отклонено 3
  кода: дубль ×2, лимит ×1» с перечнем сырых КМ, и признак в своде.

Киоск дополнительно пишет результат синка в локальный журнал (для служебного
экрана).

### 7.2 `deviceSeq` после переустановки (защита от тихой потери заявки)

Идемпотентность сервера работает по `(tenant, kiosk, deviceSeq)`: повтор с уже
виденным `deviceSeq` возвращает **существующую** заявку, не создавая новую.
Если хранилище киоска очистить (переустановка, сброс браузера), счётчик
начнётся с нуля — и первая же новая заявка будет принята за повтор старой,
то есть **молча потеряется**.

Решение: provisioning-бандл возвращает **`nextDeviceSeq`** — на единицу больше
максимального `device_seq`, уже использованного этим киоском на сервере.
Киоск инициализирует свой счётчик этим значением. При повторной привязке
после сброса нумерация продолжается, коллизий нет.

### 7.3 Пороговые значения и конфигурация

Дефолты v1 — константы сборки киоска: обновление кэша **5 мин**, предупреждение
**24 ч**, блокировка **7 суток**. Серверная настройка порогов (поля на `kiosks`)
в v1 не делается — добавляется позже без изменения клиента, когда появится
потребность.

## 8. Экраны

По прототипу `pickup-kiosk.dc.html`; две ориентации — альбом 1180×800 и
вертикаль 800×1180. Тёмная тема `@markiro/ui`, все цели касания ≥44 px
(основные 56–84 px). RU primary + EN.

1. **Ожидание** — «Возьмите продукцию для себя», зона скана QR-бейджа.
2. **Отбор** — шапка (лого, ФИО + аватар, кнопка «Не я»); зона скана КМ;
   список «Вы берёте» (фото, название, хвост кода, цена, удаление 56×56);
   счётчик и сумма; лимит N шт/день с остатком; выбор причины: **Покупка**
   (по умолчанию) / **Списание** + обязательная подпричина-чип; кнопка
   «Готово — передать администратору» (активна при ≥1 позиции и заполненной
   причине). Повторный код → янтарный баннер (~2,6 с); достигнут лимит →
   янтарная блокирующая зона вместо сканера; неизвестный товар → красная
   модалка. Цены скрыты, если `showPrices = false`.
3. **Передано** — «Заявка № N передана» (или без номера в офлайне), сводка
   (причина · штук · сумма), автосброс через 10 с.

Служебные: **Привязка** (§5.2), **Настройка сканера** (§5.3), **Blocked**
(§7, кэш старше порога).

## 9. Архитектура приложения

`apps/kiosk` — модули с одной ответственностью, тестируемые независимо:

- **`scanner/`** — `ScanSource` + две реализации (§6).
- **`domain-guard/`** — тонкий адаптер над `@markiro/domain`: различение
  бейдж/КМ, статусы `ok`/`incomplete`/`not_km`, `kmKey`.
- **`store/`** — IndexedDB: `cache` (снимок bootstrap + `fetchedAt`, включая
  бейдж-хэши и реестр операторов), `queue` (заявки + `deviceSeq`), `journal`
  (результаты синка, конфликты), `config` (токен, serverUrl, `deviceSeq`).
- **`credentials/`** — сверка бейджа за одну деривацию (§6.2) и вход в
  настройки по бейджу / логин+PIN (§5.3) поверх PHC-верификатора из
  `@markiro/domain`.
- **`sync/`** — фоновый воркер: обновление кэша, отправка очереди, backoff,
  строгий порядок по `deviceSeq`.
- **`session/`** — чистая стейт-машина экранов (по образцу `nextStationView`
  станции): `pairing → idle → cart → done`, плюс `scanner-setup` и `blocked`.
- **`ui/`** — экраны и floor-компоненты. Числовую клавиатуру для pairing можно
  поднять из `apps/station/src/ui/PinPad.tsx` (при необходимости — вынести в
  `packages/ui`).
- **`api/`** — клиент device-API: заголовок `x-kiosk-token`, базовый URL из
  конфига, типизированные вызовы `pair` / `bootstrap` / `orders`.

## 10. Триаж device-key-поверхности (обязанность воркстрима)

`docs/device-key-surface.md` (раздел «Not yet triaged») прямо закрепляет за
pickup-kiosk-воркстримом классификацию трёх модулей, которые сегодня закрыты
только `TenantGuard`, а значит **достижимы ключом любого устройства**
(включая станционный `x-api-key`):

| Модуль           | Предлагаемая классификация                                                                                                          |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `kiosks`         | **cabinet-only** (`SessionOnlyGuard`) — управление устройствами и выпуск токенов; украденное устройство не должно enroll'ить другие |
| `pickup-orders`  | **cabinet-only** — админский разбор заявок; киоск ходит через собственный `KioskDeviceGuard`                                        |
| `pickup-reasons` | **cabinet-only** — справочник редактируется в кабинете; киоску причины приезжают в bootstrap                                        |

Plan B применяет эту классификацию и обновляет `docs/device-key-surface.md`,
перенося строки из «Not yet triaged» в «Cabinet-only». Устройство киоска ничего
из перечисленного не теряет: весь его рабочий путь — `/kiosk/*` за
`KioskDeviceGuard` плюс `POST /kiosk/pair` (§5.1).

## 11. Тестирование (TDD)

- **domain:** PHC-верификатор (§6.1) — совместимость с векторами
  `apps/api/test/pin-hash.test.ts` и `apps/station/test/crypto.test.ts`
  (тот же формат, те же константы), отказ на битой/некалиброванной строке.
- **kiosk (vitest + jsdom):** стейт-машина экранов (чистая функция); оба
  `ScanSource` (эмуляция потока keydown и serial-чанков с реальным GS 0x1D);
  интеграция guard'а (валидный КМ / пропавший GS / бейдж); **сверка бейджа за
  одну деривацию** (§6.2 — совпадение, несовпадение, и что число вызовов
  PBKDF2 не растёт с размером реестра); **вход в настройки по бейджу и по
  логин+PIN** (§5.3); IndexedDB кэш и очередь (`fake-indexeddb`); sync-воркер
  (порядок по `deviceSeq`, backoff, идемпотентный повтор, атомарная замена
  кэша); пороги возраста кэша (плашка и блокировка); экран привязки (успех /
  просрочен / lockout / нет сети); экраны отбора (дубль, лимит, неизвестный
  товар, скрытие цен).
- **api (e2e, живой Postgres):** выдача кода → обмен → бандл содержит рабочий
  токен, которым проходит `GET /kiosk/bootstrap`; повторное использование кода
  → отказ; истёкший код → отказ; lockout после N попыток; **bootstrap отдаёт
  `badgeHash`/`operators` и не содержит ни одного плейнтекст-бейджа** (явный
  негативный ассерт); **все бейдж-хэши тенанта имеют общую соль** (§6.2);
  персист конфликтов виден в `PickupOrderDetailDto`; **триаж guard'ов** —
  станционный `x-api-key` получает 403 на `kiosks`/`pickup-orders`/
  `pickup-reasons` (§10).
- **admin:** плашка конфликтов в карточке заявки и признак в своде.

## 12. Принятые допущения (решения по умолчанию для плана)

Все пункты ниже — **решённые**; альтернативы записаны, чтобы план не
переоткрывал обсуждение.

1. **Настройки после привязки — за операторскими кредами** (§5.3), реестр
   операторов приезжает в bootstrap хэшами (§6.1). Идея отдельного сервисного
   PIN отклонена как дублирующая появившийся в 05b-1 механизм.
2. **Конфликты хранятся в JSONB-поле на `pickup_orders`** — проще, и выборки
   по ним в v1 не нужны (нужен только показ в карточке и признак в своде).
   Дочерняя таблица — если позже понадобится фильтрация по причинам.
3. **Пороги — константы сборки** (§7.3), серверная настройка позже.
4. **Фото товаров в v1 не показываем** — в `KioskBootstrapDto` их нет, а
   офлайн-кэш картинок заметно усложняет приложение. В списке «Вы берёте» —
   типовой плейсхолдер; название и хвост кода дают достаточную идентификацию.
   Добавление фото — отдельная поздняя доработка (URL в bootstrap + кэш).
5. **`deviceSeq` инициализируется из `nextDeviceSeq`** provisioning-бандла
   (§7.2) — закрывает тихую потерю первой заявки после переустановки.
6. **Бейдж-верификаторы солятся на тенант, PIN-верификаторы — построчно**
   (§6.2). Даёт O(1) сверку скана; компромисс осознан и записан.
7. **PHC-верификатор выносится в `packages/domain`** и используется киоском
   (§6.1). Миграция станции и API на общий модуль — вне Plan B.
8. **`POST /kiosk/orders` не меняется** — киоск шлёт предъявленный `badgeCode`,
   сервер остаётся авторитетом (§6.1).

## 13. Референсы

- План A (сервер + админка): `docs/superpowers/specs/2026-07-23-pickup-kiosk-design.md`,
  `docs/superpowers/plans/2026-07-23-pickup-kiosk-a-backend-admin.md`.
- Привязка устройств: `docs/design-briefs/07-device-commissioning.md`,
  `docs/superpowers/specs/2026-07-24-device-commissioning-design.md`.
- Реестр операторов и хэши: `docs/superpowers/specs/2026-07-24-operators-roster-design.md`,
  `apps/api/src/lib/pin-hash.ts`, `apps/api/src/modules/operators/`,
  `apps/station/src/lib/{crypto,auth}.ts`.
- Политика device-креденшелов: `docs/device-key-surface.md`.
- Прототип: `docs/design-briefs/design_handoff_markiro/prototypes/pickup-kiosk.dc.html`.
- Офлайн-first референс: `apps/station` — атомарная публикация реестра
  `src/lib/mirror.ts` (`replaceOperatorsMirror`, две слот-таблицы),
  `src/lib/{roster-sync,api-client}.ts`, `src/App.tsx` (стейт-машина),
  `src/ui/PinPad.tsx`.
- Контракт железа (референс формы для `ScanSource`): `apps/station/src/lib/hardware.ts`,
  `apps/station/src/lib/scan-source.ts`.
- Device-API: `apps/api/src/modules/kiosk/kiosk.controller.ts`,
  `apps/api/src/modules/pickup-orders/dto.ts`,
  `apps/api/src/tenancy/kiosk-device.guard.ts`,
  `apps/api/src/pickup/device-token.ts`.
