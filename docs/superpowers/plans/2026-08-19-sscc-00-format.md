# SSCC 00-Prefixed Output Format Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Все точки вывода SSCC коробов (выгрузки смен, API `/boxes`, админка, текст этикетки) отдают 20-знаковый формат с GS1 AI «00»; хранение остаётся на голых 18 цифрах.

**Architecture:** Два доменных форматтера в `@markiro/domain` (`formatSsccWithAi` → `00…` машинный, `formatSsccHri` → `(00)…` человекочитаемый) — единственный источник правила. Выгрузки смен «с коробами» получают версию формата 2 (v1 заморожена для ретраев/скачиваний старых артефактов). Потребители (API boxes, админка, эмиттеры этикеток) применяют форматтеры на выходе.

**Tech Stack:** TypeScript, pnpm workspace, Vitest, NestJS + Drizzle (apps/api), React (apps/admin), Zod.

**Spec:** `docs/superpowers/specs/2026-08-19-sscc-00-format-design.md`

## Global Constraints

- База: ветвиться от `origin/main` — модуль `apps/api/src/modules/shift-exports` и `packages/domain/src/shift-exports.ts` существуют только там.
- Хранение и обмен станция↔сервер: голые 18 цифр (НЕ менять `boxes.sscc char(18)`, `station-scans` DTO `.length(18)`, `sscc_pool`, счётчики).
- Штрихкоды на этикетках (Code128 `>;>800…` в ZPL, `!100…` в TSPL) уже корректны — НЕ трогать barcode-ветки эмиттеров.
- Форматтеры валидируют **форму** (`/^\d{18}$/`), не контрольную цифру: ингест гарантирует только длину 18, и одна битая строка не должна ронять выгрузку или страницу коробов.
- Тесты: `pnpm --filter <pkg> exec vitest run <file>`; пакеты: `@markiro/domain`, `@markiro/api`, `@markiro/admin`, `@markiro/station`.
- Коммиты частые, по одному на задачу; сообщение — conventional commits.
- Не переформатировать не затронутые строки; прогонять `pnpm --filter <pkg> lint` перед коммитом, если скрипт есть.

---

### Task 0: Ветка от origin/main

**Files:** нет (git-операции).

- [ ] **Step 1: Создать ветку и установить зависимости**

```bash
git fetch origin main
git checkout -b feature/sscc-00-format origin/main
pnpm install
```

- [ ] **Step 2: Перенести спеку и план с ветки codex/fix-admin-operations-ui**

```bash
git checkout codex/fix-admin-operations-ui -- docs/superpowers/specs/2026-08-19-sscc-00-format-design.md docs/superpowers/plans/2026-08-19-sscc-00-format.md
git add docs/superpowers/specs/2026-08-19-sscc-00-format-design.md docs/superpowers/plans/2026-08-19-sscc-00-format.md
git commit -m "docs: carry over SSCC 00-format spec and plan"
```

- [ ] **Step 3: Проверить, что базовые тесты домена зелёные**

Run: `pnpm --filter @markiro/domain test`
Expected: PASS (все существующие тесты).

---

### Task 1: Доменные форматтеры `formatSsccWithAi` / `formatSsccHri`

**Files:**

- Modify: `packages/domain/src/gs1/sscc.ts`
- Modify: `packages/domain/src/index.ts`
- Test: `packages/domain/test/sscc.test.ts`

**Interfaces:**

- Consumes: `DomainError` из `../errors.js` (уже импортирован в sscc.ts).
- Produces: `formatSsccWithAi(sscc: string): string` — вход ровно 18 цифр, выход `"00" + sscc` (20 цифр), иначе `DomainError("SSCC_FORMAT", …)`. `formatSsccHri(value: string): string` — вход 18 цифр ИЛИ 20 цифр с `00`, выход `"(00)" + <18 цифр>`, иначе `DomainError("SSCC_FORMAT", …)`.

- [ ] **Step 1: Написать падающие тесты**

Добавить в конец `packages/domain/test/sscc.test.ts` (импорт расширить: `formatSsccWithAi, formatSsccHri` из `../src/index.js`; `DomainError` уже может быть импортирован — проверить и добавить при отсутствии):

```ts
describe("formatSsccWithAi", () => {
  it("prepends the 00 application identifier to a bare 18-digit SSCC", () => {
    expect(formatSsccWithAi("346006820000000014")).toBe("00346006820000000014");
  });

  it.each([
    ["17 digits", "34600682000000001"],
    ["19 digits", "3460068200000000140"],
    ["already 00-prefixed 20 digits", "00346006820000000014"],
    ["non-digits", "34600682000000001X"],
    ["empty", ""],
  ])("throws SSCC_FORMAT on %s", (_name, value) => {
    expect(() => formatSsccWithAi(value)).toThrowError(DomainError);
    try {
      formatSsccWithAi(value);
    } catch (error) {
      expect((error as DomainError).code).toBe("SSCC_FORMAT");
    }
  });
});

describe("formatSsccHri", () => {
  it("formats a bare 18-digit SSCC as (00)…", () => {
    expect(formatSsccHri("346006820000000014")).toBe("(00)346006820000000014");
  });

  it("accepts an already 00-prefixed 20-digit value and formats the bare part", () => {
    expect(formatSsccHri("00346006820000000014")).toBe("(00)346006820000000014");
  });

  it("does NOT strip a leading 00 from a value that is itself a bare 18-digit SSCC", () => {
    // 18 digits starting with 00 is a bare SSCC (extension digit 0, prefix 0…),
    // not an AI-prefixed 16-digit tail.
    expect(formatSsccHri("004601234560000017")).toBe("(00)004601234560000017");
  });

  it.each([
    ["17 digits", "34600682000000001"],
    ["19 digits", "3460068200000000140"],
    ["20 digits without 00", "12346006820000000014"],
    ["non-digits", "34600682000000001X"],
    ["empty", ""],
  ])("throws SSCC_FORMAT on %s", (_name, value) => {
    expect(() => formatSsccHri(value)).toThrowError(DomainError);
  });
});
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `pnpm --filter @markiro/domain exec vitest run test/sscc.test.ts`
Expected: FAIL — `formatSsccWithAi is not a function` (или ошибка импорта).

- [ ] **Step 3: Реализация**

В конец `packages/domain/src/gs1/sscc.ts`:

```ts
const BARE_SSCC_SHAPE = /^\d{18}$/;

/**
 * The 20-character machine form Chestny ZNAK exchanges expect: GS1
 * application identifier `00` + the bare 18 digits. Validates SHAPE only
 * (exactly 18 digits), not the check digit: ingest guarantees length, and a
 * single corrupt row must not fail a whole export. Rejecting non-18-digit
 * input is what prevents double-prefixing.
 */
export function formatSsccWithAi(sscc: string): string {
  if (!BARE_SSCC_SHAPE.test(sscc)) {
    throw new DomainError("SSCC_FORMAT", `not a bare 18-digit SSCC: "${sscc}"`);
  }
  return `00${sscc}`;
}

/**
 * The human-readable GS1 HRI form `(00)…` for labels and admin screens.
 * Accepts either the bare 18 digits or the 20-character `00…` machine form
 * (what `/boxes` responses now carry), so display code never has to know
 * which one it holds.
 */
export function formatSsccHri(value: string): string {
  const bare = value.length === 20 && value.startsWith("00") ? value.slice(2) : value;
  if (!BARE_SSCC_SHAPE.test(bare)) {
    throw new DomainError("SSCC_FORMAT", `not an SSCC: "${value}"`);
  }
  return `(00)${bare}`;
}
```

В `packages/domain/src/index.ts` расширить существующий экспорт из `./gs1/sscc.js`:

```ts
export {
  buildSscc,
  formatSsccHri,
  formatSsccWithAi,
  isValidSscc,
  parseScannedSscc,
  parseSscc,
  ssccSerialCapacity,
} from "./gs1/sscc.js";
```

- [ ] **Step 4: Тесты зелёные**

Run: `pnpm --filter @markiro/domain exec vitest run test/sscc.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/domain/src/gs1/sscc.ts packages/domain/src/index.ts packages/domain/test/sscc.test.ts
git commit -m "feat(domain): add SSCC 00-AI and HRI formatters"
```

---

### Task 2: HRI в текстовых элементах этикеток (эмиттеры ZPL/TSPL)

**Files:**

- Modify: `packages/domain/src/labels/model.ts`
- Modify: `packages/domain/src/labels/zpl.ts` (case `"field"` в `generateZpl`, ~строка 315)
- Modify: `packages/domain/src/labels/tspl.ts` (case `"field"` в `generateTspl`, ~строка 350)
- Modify: `packages/domain/src/index.ts`
- Test: `packages/domain/test/labels-zpl.test.ts`, `packages/domain/test/labels-tspl.test.ts`

**Interfaces:**

- Consumes: `formatSsccHri` из Task 1 (`../gs1/sscc.js`); `LabelField`, `sampleLabelData` уже в model.ts.
- Produces: `labelFieldDisplayValue(field: LabelField, data: Record<LabelField, string>): string` — для `field === "sscc"` с 18-цифровым значением возвращает `(00)…`, иначе значение как есть (толерантно: пустые/произвольные значения не роняют генерацию). Используется всеми рендерами текста поля (ZPL, TSPL, превью админки в Task 7).

- [ ] **Step 1: Написать падающие тесты**

В `packages/domain/test/labels-zpl.test.ts` рядом с существующим тестом про GS1-128 (строка ~209) добавить:

```ts
it("renders a field element bound to sscc as GS1 HRI (00)…", async () => {
  const spec = parseLabelTemplate({
    widthMm: 100,
    heightMm: 60,
    dpi: 203,
    elements: [{ kind: "field", id: "f", field: "sscc", xMm: 5, yMm: 5, fontSizePt: 10 }],
  });
  const zpl = await generateZpl(spec, { ...sampleLabelData(), sscc: "004601234560000017" });
  expect(zpl).toContain("(00)004601234560000017");
  expect(zpl).not.toContain("^FD004601234560000017^FS");
});

it("leaves a non-sscc field element's text untouched", async () => {
  const spec = parseLabelTemplate({
    widthMm: 100,
    heightMm: 60,
    dpi: 203,
    elements: [{ kind: "field", id: "f", field: "gtin", xMm: 5, yMm: 5, fontSizePt: 10 }],
  });
  const zpl = await generateZpl(spec, sampleLabelData());
  expect(zpl).not.toContain("(00)");
});
```

Примечание: скопировать точную форму `parseLabelTemplate`/элементов из соседних тестов файла (структура `field`-элемента — как у существующих текстовых тестов; если обязательны другие свойства, добавить по образцу). Аналогичную пару тестов добавить в `packages/domain/test/labels-tspl.test.ts` с `generateTspl` и проверкой `expect(tspl).toContain("(00)004601234560000017")`.

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `pnpm --filter @markiro/domain exec vitest run test/labels-zpl.test.ts test/labels-tspl.test.ts`
Expected: FAIL — в выводе голые 18 цифр без `(00)`.

- [ ] **Step 3: Реализация**

В `packages/domain/src/labels/model.ts` (после `sampleLabelData`), импортировав `formatSsccHri` из `../gs1/sscc.js`:

```ts
/**
 * Resolves a `field` element's display text. The one field-specific rule
 * lives here: `sscc` renders in GS1 HRI form `(00)…` — the barcode emitters
 * already add the AI to the encoded data, and Chestny ZNAK requires the
 * human-readable form to show it too. Tolerant by design: preview/generation
 * may run with empty or arbitrary data, so a value that isn't 18 digits is
 * returned unchanged rather than throwing.
 */
export function labelFieldDisplayValue(
  field: LabelField,
  data: Record<LabelField, string>,
): string {
  const value = data[field] ?? "";
  return field === "sscc" && /^\d{18}$/.test(value) ? formatSsccHri(value) : value;
}
```

В `packages/domain/src/index.ts` добавить `labelFieldDisplayValue` в экспорт из `./labels/model.js`.

В `packages/domain/src/labels/zpl.ts`, в `generateZpl`:

```ts
      case "field":
        lines.push(
          await renderTextLikeElement(element, labelFieldDisplayValue(element.field, data), spec, deps),
        );
        break;
```

(импортировать `labelFieldDisplayValue` из `./model.js` — там уже есть импорты из model). То же самое в `packages/domain/src/labels/tspl.ts` в `generateTspl` (case `"field"`, ~строка 350).

- [ ] **Step 4: Тесты зелёные, включая станцию (box-label использует эмиттеры)**

Run: `pnpm --filter @markiro/domain test && pnpm --filter @markiro/station test`
Expected: PASS. Если тесты станции/домена ассертят старый вывод текстового поля sscc (голые 18 цифр) — обновить ожидания на `(00)…`.

- [ ] **Step 5: Commit**

```bash
git add packages/domain/src/labels/model.ts packages/domain/src/labels/zpl.ts packages/domain/src/labels/tspl.ts packages/domain/src/index.ts packages/domain/test/labels-zpl.test.ts packages/domain/test/labels-tspl.test.ts
git commit -m "feat(domain): render sscc field text as GS1 HRI (00)… on labels"
```

---

### Task 3: Выгрузки смен — версия 2 форматов «с коробами» (домен)

**Files:**

- Modify: `packages/domain/src/shift-exports.ts`
- Modify: `packages/domain/src/index.ts` (экспорт `LEGACY_SHIFT_EXPORT_FORMATS` не нужен наружу — НЕ добавлять; проверить, что ничего не сломалось)
- Test: `packages/domain/test/shift-exports.test.ts`

**Interfaces:**

- Consumes: `formatSsccWithAi` из Task 1.
- Produces: `ShiftExportFormatDescriptor.version: 1 | 2`; `SHIFT_EXPORT_FORMATS` (рекламируемый список) содержит `shift_txt_flat@1`, `shift_txt_boxes@2`, `shift_csv_flat@1`, `shift_csv_boxes@2`; `getShiftExportFormat(id, version)` резолвит И рекламируемые, И легаси `shift_txt_boxes@1` / `shift_csv_boxes@1` (замороженное поведение — 18 цифр); `RenderShiftExportInput.formatVersion: number`.

- [ ] **Step 1: Написать падающие тесты**

В `packages/domain/test/shift-exports.test.ts` (фикстуры вверху файла — `sscc: "001234567890123456"` и `"009876543210123456"` — 18-значные, форма валидна, менять не нужно) добавить:

```ts
describe("boxes format version 2 (00-prefixed SSCC)", () => {
  it("advertises version 2 for boxes formats and version 1 for flat", () => {
    const byId = new Map(SHIFT_EXPORT_FORMATS.map((f) => [f.id, f.version]));
    expect(byId.get("shift_txt_boxes")).toBe(2);
    expect(byId.get("shift_csv_boxes")).toBe(2);
    expect(byId.get("shift_txt_flat")).toBe(1);
    expect(byId.get("shift_csv_flat")).toBe(1);
  });

  it("still resolves the frozen v1 boxes formats for old artifacts/retries", () => {
    expect(getShiftExportFormat("shift_txt_boxes", 1).version).toBe(1);
    expect(getShiftExportFormat("shift_csv_boxes", 1).version).toBe(1);
  });

  it("renders TXT v2 box headers as 20-digit 00-prefixed SSCC", () => {
    const parts = renderShiftExport({
      formatId: "shift_txt_boxes",
      formatVersion: 2,
      productName: "Товар",
      shiftDate: "2026-08-19",
      maxLines: null,
      source: {
        mode: "boxes",
        boxes: [{ sscc: "001234567890123456", codes: ["KM-1", "KM-2"] }],
      },
    });
    expect(new TextDecoder().decode(parts[0]!.bytes)).toBe("00001234567890123456\nKM-1\nKM-2\n\n");
  });

  it("renders CSV v2 box_sscc column as 20-digit 00-prefixed SSCC", () => {
    const parts = renderShiftExport({
      formatId: "shift_csv_boxes",
      formatVersion: 2,
      productName: "Товар",
      shiftDate: "2026-08-19",
      maxLines: null,
      source: {
        mode: "boxes",
        boxes: [{ sscc: "001234567890123456", codes: ["KM-1"] }],
      },
    });
    const body = new TextDecoder().decode(parts[0]!.bytes.slice(3)); // strip BOM
    expect(body).toBe("box_sscc;code\r\n00001234567890123456;KM-1\r\n");
  });

  it("keeps v1 boxes rendering frozen at bare 18 digits", () => {
    const parts = renderShiftExport({
      formatId: "shift_txt_boxes",
      formatVersion: 1,
      productName: "Товар",
      shiftDate: "2026-08-19",
      maxLines: null,
      source: {
        mode: "boxes",
        boxes: [{ sscc: "001234567890123456", codes: ["KM-1"] }],
      },
    });
    expect(new TextDecoder().decode(parts[0]!.bytes)).toBe("001234567890123456\nKM-1\n\n");
  });
});
```

Существующие тесты файла зовут `renderShiftExport` с `formatVersion: 1` для boxes-форматов — они остаются валидными (легаси-резолв) и менять их НЕ нужно, кроме тестов, ассертящих состав `SHIFT_EXPORT_FORMATS` (если такие есть — обновить версии boxes-форматов на 2).

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `pnpm --filter @markiro/domain exec vitest run test/shift-exports.test.ts`
Expected: FAIL — версии равны 1, вывод без `00`-префикса.

- [ ] **Step 3: Реализация**

В `packages/domain/src/shift-exports.ts`:

1. Импорт: `import { formatSsccWithAi } from "./gs1/sscc.js";`
2. Дескриптор: `version: 1;` → `version: 1 | 2;` в `ShiftExportFormatDescriptor`; `formatVersion: 1;` → `formatVersion: number;` в `RenderShiftExportInput`.
3. В `SHIFT_EXPORT_FORMATS` у `shift_txt_boxes` и `shift_csv_boxes` сменить `version: 1` на `version: 2` (лейблы не менять).
4. После `SHIFT_EXPORT_FORMATS` добавить:

```ts
/**
 * Frozen v1 descriptors for the boxes formats: version 2 switched the SSCC
 * to the 20-digit 00-prefixed form, but already-created v1 exports must keep
 * re-rendering (retry) and re-downloading byte-identically. Not advertised —
 * `SHIFT_EXPORT_FORMATS` is what the UI offers for NEW exports.
 */
const LEGACY_SHIFT_EXPORT_FORMATS = Object.freeze([
  Object.freeze({
    id: "shift_txt_boxes",
    version: 1,
    label: "[TXT][С коробами] Отчет смены",
    extension: "txt",
    mimeType: "text/plain; charset=utf-8",
    boxMode: "boxes",
  } as const),
  Object.freeze({
    id: "shift_csv_boxes",
    version: 1,
    label: "[CSV][С коробами] Отчет смены",
    extension: "csv",
    mimeType: "text/csv; charset=utf-8",
    boxMode: "boxes",
  } as const),
] as const satisfies readonly ShiftExportFormatDescriptor[]);
```

5. `getShiftExportFormat` ищет в обоих списках:

```ts
export function getShiftExportFormat(id: string, version: number): ShiftExportFormatDescriptor {
  const descriptor = [...SHIFT_EXPORT_FORMATS, ...LEGACY_SHIFT_EXPORT_FORMATS].find(
    (candidate) => candidate.id === id && candidate.version === version,
  );

  if (!descriptor) {
    throw new ShiftExportDomainError("FORMAT_NOT_FOUND");
  }

  return descriptor;
}
```

6. В `createBlocks`, ветка boxes-режима — SSCC через версию дескриптора:

```ts
  return source.boxes.map((box) => {
    const ssccOut = descriptor.version >= 2 ? formatSsccWithAi(box.sscc) : box.sscc;
    const lines =
      descriptor.extension === "txt"
        ? [ssccOut, ...box.codes, ""]
        : box.codes.map((code) => `${csvField(ssccOut)};${csvField(code)}`);
    // остальное без изменений (physicalLineCount считать от lines, как сейчас)
```

- [ ] **Step 4: Тесты зелёные**

Run: `pnpm --filter @markiro/domain test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/domain/src/shift-exports.ts packages/domain/test/shift-exports.test.ts
git commit -m "feat(domain): shift export boxes formats v2 with 00-prefixed SSCC"
```

---

### Task 4: API выгрузок — принять версию 2, валидировать формат при создании

**Files:**

- Modify: `apps/api/src/modules/shift-exports/dto.ts`
- Modify: `apps/api/src/modules/shift-exports/shift-exports.service.ts`
- Test: `apps/api/test/shift-exports-openapi.test.ts`, `apps/api/test/shift-exports.e2e.test.ts`, `apps/api/test/shift-export-runner.test.ts`, `apps/api/test/jobs-shift-exports.test.ts`

**Interfaces:**

- Consumes: `SHIFT_EXPORT_FORMATS`, `getShiftExportFormat` из `@markiro/domain` (Task 3).
- Produces: `createShiftExportSchema.formatVersion: z.number().int().min(1)`; `create()` отвергает `BadRequestException`-ом любую пару (formatId, formatVersion), которой нет в рекламируемом `SHIFT_EXPORT_FORMATS` (т.е. новые v1-boxes создать нельзя, ретрай существующих работает через легаси-резолв в runner); `ShiftExportDto.formatVersion: number`.

- [ ] **Step 1: Написать падающие e2e-тесты**

В `apps/api/test/shift-exports.e2e.test.ts` по образцу существующих кейсов создания добавить:

```ts
it("rejects creating a boxes export with the superseded format version 1", async () => {
  // arrange как в соседних тестах создания экспорта (закрытая смена и т.п.)
  const response = await request(/* как в соседних тестах */)
    .post(`/shifts/${shiftId}/exports`)
    .send({
      formatId: "shift_txt_boxes",
      formatVersion: 1,
      maxLines: null,
      idempotencyKey: crypto.randomUUID(),
    });
  expect(response.status).toBe(400);
});

it("creates a boxes export at format version 2", async () => {
  const response = await request(/* как в соседних тестах */)
    .post(`/shifts/${shiftId}/exports`)
    .send({
      formatId: "shift_txt_boxes",
      formatVersion: 2,
      maxLines: null,
      idempotencyKey: crypto.randomUUID(),
    });
  expect(response.status).toBe(201);
  expect(response.body.formatVersion).toBe(2);
});
```

(точную обвязку — auth, фабрики смены — скопировать из соседних тестов этого файла; они уже создают экспорты boxes-форматов).

- [ ] **Step 2: Убедиться, что новые тесты падают**

Run: `pnpm --filter @markiro/api exec vitest run test/shift-exports.e2e.test.ts`
Expected: новые кейсы FAIL (v1 сейчас проходит zod-литерал, v2 отвергается литералом `z.literal(1)`).

- [ ] **Step 3: Реализация**

`apps/api/src/modules/shift-exports/dto.ts`:

```ts
formatVersion: z.number().int().min(1),
```

Типы: `ShiftExportDto.formatVersion: 1` → `formatVersion: number`. OpenAPI:

- `shiftExportFormatOpenApiSchema.properties.version`: `{ type: "integer", enum: [1, 2] }`
- `createShiftExportOpenApiSchema.properties.formatVersion`: `{ type: "integer", minimum: 1 }`
- `shiftExportOpenApiSchema.properties.formatVersion`: `{ type: "integer", minimum: 1 }`

`apps/api/src/modules/shift-exports/shift-exports.service.ts`:

- Импорт: добавить `BadRequestException` в импорт из `@nestjs/common`; `SHIFT_EXPORT_FORMATS` уже импортирован.
- В начале `create()` (до транзакции):

```ts
const advertised = SHIFT_EXPORT_FORMATS.some(
  (format) => format.id === input.formatId && format.version === input.formatVersion,
);
if (!advertised) {
  throw new BadRequestException("Unknown or superseded export format version");
}
```

- В `toDtos` (строка ~254) убрать каст: `formatVersion: row.formatVersion as 1` → `formatVersion: row.formatVersion`.

- [ ] **Step 4: Прогнать все тесты выгрузок API, обновить фикстуры**

Run: `pnpm --filter @markiro/api exec vitest run test/shift-exports-openapi.test.ts test/shift-exports.e2e.test.ts test/shift-export-runner.test.ts test/jobs-shift-exports.test.ts test/shift-export-source.test.ts`
Expected: PASS после обновлений:

- openapi-тест: ожидания `enum: [1]` → по новым схемам выше.
- e2e/runner-тесты, создающие boxes-экспорты с `formatVersion: 1`, перевести на `formatVersion: 2`; ожидаемое содержимое артефактов boxes-форматов — SSCC с префиксом `00` (20 цифр). Тесты runner'а, вызывающие рендер напрямую с уже сохранённой v1-строкой, оставить на v1 (замороженное поведение).

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/modules/shift-exports apps/api/test
git commit -m "feat(api): accept shift export format v2, reject superseded versions"
```

---

### Task 5: Админка — диалог выгрузок отправляет версию выбранного формата

**Files:**

- Modify: `apps/admin/src/pages/shifts/shift-exports-api.ts`
- Modify: `apps/admin/src/pages/shifts/ShiftExportsDialog.tsx`
- Test: `apps/admin/test/shift-exports-dialog.test.tsx`, `apps/admin/test/shift-exports-api.test.tsx`

**Interfaces:**

- Consumes: `GET /shift-exports/formats` теперь отдаёт boxes-форматы с `version: 2`.
- Produces: `CreateShiftExportInput.formatVersion: number`; `ShiftExportDto.formatVersion: number`; сабмит диалога отправляет `formatVersion` выбранного дескриптора (не хардкод 1).

- [ ] **Step 1: Написать падающий тест**

В `apps/admin/test/shift-exports-dialog.test.tsx` по образцу существующего теста сабмита: замокать `GET /shift-exports/formats` списком, где `shift_txt_boxes` имеет `version: 2`, выбрать его, сабмитнуть и заассертить тело POST:

```ts
expect(capturedBody.formatVersion).toBe(2);
expect(capturedBody.formatId).toBe("shift_txt_boxes");
```

(механику перехвата запроса скопировать из существующих тестов файла — там уже есть моки formats/create).

- [ ] **Step 2: Убедиться, что тест падает**

Run: `pnpm --filter @markiro/admin exec vitest run test/shift-exports-dialog.test.tsx`
Expected: FAIL — `formatVersion` равен 1 (хардкод).

- [ ] **Step 3: Реализация**

`apps/admin/src/pages/shifts/shift-exports-api.ts`: в `ShiftExportDto` и `CreateShiftExportInput` поменять `formatVersion: 1` → `formatVersion: number`.

`apps/admin/src/pages/shifts/ShiftExportsDialog.tsx`, в `submit`:

```ts
  const submit = async (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    if (!canSubmit || !formatId) return;
    const selectedFormat = (formats.data ?? []).find((format) => format.id === formatId);
    if (!selectedFormat) return;
    // A new deliberate submission after a failed request starts a new idempotency scope.
    const requestIdempotencyKey = idempotencyKey.current ?? crypto.randomUUID();
    idempotencyKey.current = requestIdempotencyKey;
    setError(null);
    try {
      await create.mutateAsync({
        shiftId: shift.id,
        input: {
          formatId,
          formatVersion: selectedFormat.version,
          maxLines: parsedLineLimit,
          idempotencyKey: requestIdempotencyKey,
        },
      });
```

(остальное тело `submit` без изменений).

- [ ] **Step 4: Тесты зелёные**

Run: `pnpm --filter @markiro/admin exec vitest run test/shift-exports-dialog.test.tsx test/shift-exports-api.test.tsx`
Expected: PASS (обновить фикстуры `formatVersion: 1` там, где мокаются boxes-форматы, на 2 — история экспортов с v1 остаётся валидной, её не трогать).

- [ ] **Step 5: Commit**

```bash
git add apps/admin/src/pages/shifts/shift-exports-api.ts apps/admin/src/pages/shifts/ShiftExportsDialog.tsx apps/admin/test/shift-exports-dialog.test.tsx apps/admin/test/shift-exports-api.test.tsx
git commit -m "feat(admin): send selected shift export format version"
```

---

### Task 6: API `/boxes` — SSCC в 20-знаковом формате

**Files:**

- Modify: `apps/api/src/modules/boxes/boxes.service.ts` (метод `toDto`)
- Modify: `apps/api/src/modules/boxes/dto.ts` (док-коммент `BoxDto.sscc`)
- Test: `apps/api/test/boxes.e2e.test.ts`

**Interfaces:**

- Consumes: `formatSsccWithAi` из `@markiro/domain` (Task 1).
- Produces: `BoxDto.sscc: string | null` — 20 знаков `00…` для закрытого короба, `null` для открытого. Ингест со станции (`station-scans`) НЕ меняется — там по-прежнему 18.

- [ ] **Step 1: Обновить e2e-ожидания (падающий тест)**

В `apps/api/test/boxes.e2e.test.ts`: фикстуры закрытия коробов (`sscc: "123456789012345675"` и т.п.) оставить 18-значными — так их шлёт станция. Ожидания DTO поменять:

```ts
expect(box.sscc).toBe("00123456789012345675");
```

(строка ~288 и любые другие ассерты на `sscc` в ответе `/boxes`; тип локального интерфейса на строке ~252 не меняется — `sscc: string`).

- [ ] **Step 2: Убедиться, что тест падает**

Run: `pnpm --filter @markiro/api exec vitest run test/boxes.e2e.test.ts`
Expected: FAIL — получено 18 цифр без `00`.

- [ ] **Step 3: Реализация**

`apps/api/src/modules/boxes/boxes.service.ts` — импортировать `formatSsccWithAi` из `@markiro/domain`, в `toDto`:

```ts
  private toDto(row: BoxRow): BoxDto {
    return {
      id: row.id,
      sscc: row.sscc === null ? null : formatSsccWithAi(row.sscc),
      terminalId: row.terminalId,
      operatorId: row.operatorId,
      lineName: row.lineName,
      itemCount: row.itemCount,
      closedAt: row.closedAt,
      contentsChangedAfterClose: row.contentsChangedAfterClose,
      disassembledAt: row.disassembledAt,
    };
  }
```

(порядок полей — как в текущем файле). В `apps/api/src/modules/boxes/dto.ts` дополнить док-коммент `BoxDto.sscc`: `/** 20-значный код с GS1 AI "00" (требование Честного знака); в БД хранится голый 18-значный SSCC. */`

- [ ] **Step 4: Тесты зелёные**

Run: `pnpm --filter @markiro/api exec vitest run test/boxes.e2e.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/modules/boxes apps/api/test/boxes.e2e.test.ts
git commit -m "feat(api): return box SSCC in 20-digit 00-prefixed form"
```

---

### Task 7: Админка — HRI в таблице коробов и в превью редактора этикеток

**Files:**

- Modify: `apps/admin/src/pages/boxes/index.tsx` (колонка `sscc`)
- Modify: `apps/admin/src/pages/labels/editor/PreviewPane.tsx` (строка ~86)
- Modify: `apps/admin/src/pages/labels/editor/renderer.ts` (строки ~130 и ~412)
- Test: `apps/admin/test/boxes.test.tsx`, `apps/admin/test/labels-editor.test.tsx` (если ассертит текст sscc-поля)

**Interfaces:**

- Consumes: `formatSsccHri`, `labelFieldDisplayValue` из `@markiro/domain` (Tasks 1–2); `BoxDto.sscc` теперь 20 знаков `00…` (Task 6).
- Produces: таблица коробов показывает `(00)123456789012345675`; превью этикетки показывает текст sscc-поля так же, как напечатает принтер.

- [ ] **Step 1: Написать падающие тесты**

`apps/admin/test/boxes.test.tsx`: фикстуру `CLOSED_BOX.sscc` сменить на серверный формат `"00123456789012345675"`, ожидание рендера:

```ts
expect(table.getByText("(00)123456789012345675")).toBeDefined();
```

(строка ~149; кейс с `sscc: null` не трогать — рендерит `—`).

- [ ] **Step 2: Убедиться, что тест падает**

Run: `pnpm --filter @markiro/admin exec vitest run test/boxes.test.tsx`
Expected: FAIL.

- [ ] **Step 3: Реализация**

`apps/admin/src/pages/boxes/index.tsx` — импортировать `formatSsccHri` из `@markiro/domain`, колонка:

```tsx
      {
        key: "sscc",
        title: t("pages.boxes.table.sscc"),
        mono: true,
        render: (row) => (row.sscc ? formatSsccHri(row.sscc) : "—"),
      },
```

`apps/admin/src/pages/labels/editor/PreviewPane.tsx:86` и `apps/admin/src/pages/labels/editor/renderer.ts:130` — заменить резолв текста поля:

```ts
return element.kind === "text" ? element.text : labelFieldDisplayValue(element.field, data);
```

`apps/admin/src/pages/labels/editor/renderer.ts:412`:

```ts
      case "field":
        drawTextElement(ctx, element, labelFieldDisplayValue(element.field, data), scale);
```

(импорт `labelFieldDisplayValue` из `@markiro/domain` в оба файла).

- [ ] **Step 4: Тесты зелёные**

Run: `pnpm --filter @markiro/admin test`
Expected: PASS (если `labels-editor.test.tsx` ассертит текст sscc-поля голыми 18 цифрами — обновить на `(00)…`).

- [ ] **Step 5: Commit**

```bash
git add apps/admin/src/pages/boxes/index.tsx apps/admin/src/pages/labels/editor apps/admin/test
git commit -m "feat(admin): show box SSCC in GS1 HRI form"
```

---

### Task 8: Регрессия сканирования — 20-знаковый скан закреплён тестом

**Files:**

- Test: `apps/station/test/print-verification.test.tsx`

**Interfaces:**

- Consumes: существующий `PrintVerification` (`apps/station/src/ui/PrintVerification.tsx`) и `parseScannedSscc` — поведение НЕ меняется.
- Produces: явный тест: скан `]C1` + `00` + 18 цифр (то, что реально вернёт сканер с GS1-128 этикетки) подтверждает печать короба.

- [ ] **Step 1: Проверить существующее покрытие и дописать кейс**

Открыть `apps/station/test/print-verification.test.tsx`. Если кейса со сканом `"]C100" + sscc` (или `"00" + sscc`) нет — добавить по образцу существующего успешного кейса:

```tsx
it("verifies when the scanner returns the full GS1-128 payload (]C1 + 00 + 18 digits)", () => {
  // arrange идентично существующему успешному кейсу, но скан:
  emitScan(`]C100${BOX_SSCC}`); // BOX_SSCC — 18-значная фикстура файла
  expect(onVerified).toHaveBeenCalled();
});
```

(имена хелперов — `emitScan`/фикстуры — взять из этого же файла).

- [ ] **Step 2: Тест зелёный сразу (регрессионный)**

Run: `pnpm --filter @markiro/station exec vitest run test/print-verification.test.tsx`
Expected: PASS без изменений кода — фиксируем поведение.

- [ ] **Step 3: Commit**

```bash
git add apps/station/test/print-verification.test.tsx
git commit -m "test(station): pin 00-prefixed SSCC scan verification"
```

---

### Task 9: Финальная верификация

- [ ] **Step 1: Полный прогон затронутых пакетов**

```bash
pnpm --filter @markiro/domain test && pnpm --filter @markiro/api test && pnpm --filter @markiro/admin test && pnpm --filter @markiro/station test
```

Expected: все PASS.

- [ ] **Step 2: Линт/типы**

```bash
pnpm --filter @markiro/domain lint && pnpm --filter @markiro/api lint && pnpm --filter @markiro/admin lint && pnpm --filter @markiro/station lint
```

Expected: чисто (если lint-скрипта в пакете нет — пропустить этот пакет).

- [ ] **Step 3: Итоговый коммит остатков (если есть) и сводка**

Убедиться `git status` чист; несмёрженные правки закоммитить в задачу, к которой относятся. Работа готова к PR в `main` (использовать superpowers:finishing-a-development-branch).
