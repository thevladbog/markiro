PIN-клавиатура для входа оператора на станции линии — вместо логина/пароля на touch-экране.

```jsx
<PinPad length={4} onSubmit={(pin) => login(pin)} />
```

Клавиши 64px (перчатка), точки-индикаторы квадратные — характер «прибора».
