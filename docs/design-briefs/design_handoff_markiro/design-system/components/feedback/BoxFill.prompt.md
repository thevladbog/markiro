Box-fill — сигнатурный компонент продукта: короб как сетка ячеек, заполняется по мере сканирования.

```jsx
<BoxFill filled={14} total={20} cellSize={36} mode="floor" />
```

Ячейки квадратные (радиус 2), заполнение статусным цветом. На станции — рядом со счётчиком смены.
