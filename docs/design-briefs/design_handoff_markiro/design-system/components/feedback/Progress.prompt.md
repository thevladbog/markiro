ProgressBar и RingCounter — один файл.

```jsx
<ProgressBar label="Задание" value={47213} max={50000} showValue />
<RingCounter value={14} max={20} size={140} label="короб" kind="ok" />
```

Числа всегда mono + tabular-nums.
