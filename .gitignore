.DS_Store
node_modules/
dist/
.astro/
.turbo/
coverage/
install.log
.env
.env.production
.markiro-releases/
deploy/production/.caddy-data/
deploy/production/.caddy-config/

# Sibling agent worktrees (also in .prettierignore)
.worktrees/

# Local Superpowers execution artifacts; reviewed specs and plans live under docs/.
/.superpowers/

# Local Terraform artifacts; examples and dependency locks remain committed.
.terraform/
*.tfstate*
*.tfplan
*.auto.tfvars
backend.hcl
backend.tf
!.terraform.lock.hcl
!*.example

# Pencil stores a cloud-scoped file token in this local design container.
docs/design-briefs/landing.pen

# Local pnpm content-addressable store when the repo is installed with --store-dir here.
.pnpm-store/

# Browser-automation scratch output and local UI screenshots; committed design
# assets live under docs/design-briefs/ with explicit names.
.playwright-mcp/
/*.png
docs/design-briefs/screenshot-*.png

# Private first-customer evidence packages; tooling remains tracked under tools/.
/evidence/
