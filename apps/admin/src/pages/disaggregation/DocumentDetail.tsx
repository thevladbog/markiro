/**
 * Stub for the disaggregation document detail route (`/disaggregation/:id`).
 * Task 9 only needs the route to compile so the list page's create flow
 * (`navigate(`/disaggregation/${doc.id}`)`) has somewhere to land -- Task 10
 * replaces this with the real lines editor/apply/cancel view.
 */
export function DisaggregationDocumentPage() {
  return null;
}
